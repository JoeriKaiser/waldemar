---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<header>
  <div class="container-center px-[var(--space-xl)]">
    <h1>{title}</h1>
    {description && <p>{description}</p>}
  </div>
</header>
